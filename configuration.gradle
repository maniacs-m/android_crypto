// Defines configuration used across the library project.
def buildConfig = [
        compileSdkVersion: 25,
        minSdkVersion    : 8,
        targetSdkVersion : 25,
        buildToolsVersion: '25.0.2'
]

def testConfig = [
        instrumentationRunner: 'android.support.test.runner.AndroidJUnitRunner'
]

def libraryConfig = [
        version: '0.1.2'
]

def mavenConfig = [
        localUrl                 : System.getenv('MAVEN_URL_LOCAL'),
        groupId                  : 'universum.studios.android',
        artifactId               : 'crypto',
        version                  : libraryConfig.version,
        packaging                : 'pom',
        projectName              : 'Android Crypto',
        projectDescription       : 'Cryptographic tools for the Android platform.',
        projectUrl               : 'https://github.com/universum-studios/android_crypto',
        projectInceptionYear     : '2017',
        projectIssueManagementUrl: 'https://github.com/universum-studios/android_crypto/issues'
]

def bintrayConfig = [
        publish                  : false,
        packageName              : mavenConfig.groupId + ':' + mavenConfig.artifactId,
        packageLabels            : ['android', 'encryption', 'decryption'],
        packageVersionName       : mavenConfig.version,
        packageVersionDescription: 'Pre-release patch.',
        packageVersionTag        : mavenConfig.version
]

def codacyConfig = [
        // This path must be relative to the codacy.gradle file placed in the /library directory.
        tokensPath : '../../codacy.tokens',
        projectName: 'android_crypto'
]

def ciConfig = [
        travis: Boolean.parseBoolean(System.getenv('TRAVIS'))
]

ext.config = [
        "build"  : buildConfig,
        "test"   : testConfig,
        "library": libraryConfig,
        "maven"  : mavenConfig,
        "bintray": bintrayConfig,
        "codacy" : codacyConfig,
        "ci"     : ciConfig
]